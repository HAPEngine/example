CMAKE_MINIMUM_REQUIRED(VERSION 3.5)
PROJECT (hap_example)

FILE(GLOB_RECURSE HAP_EXAMPLE_HEADERS src/*.h)
FILE(GLOB HAP_EXAMPLE_SOURCES src/*.c)

ADD_LIBRARY(hap_example SHARED ${HAP_EXAMPLE_SOURCES} ${HAP_EXAMPLE_HEADERS})